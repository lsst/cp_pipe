pipelineYaml: "${CP_PIPE_DIR}/pipelines/${INSTRUMENT}/cpPtc.yaml#cpPtcSolve"

project: "${TICKET}"
campaign: "${TICKET}"
submitPath: $SCRATCH/submit/{outputRun}

payload:
  payloadName: "${INSTRUMENT}_${TICKET}_ptc"
  output: "${USER_CALIB_PREFIX}${INSTRUMENT}/calib/${TICKET}/${TAG}/ptcGen.${RERUN}"
  butlerConfig: "${REPO}"
  inCollection: "u/abrought/LSSTCam/calib/DM-49175/test/ptcGen.20250309c,${CALIB_COLLECTIONS}" # ${USER_CALIB_PREFIX}${INSTRUMENT}/calib/${TICKET},${RAW_COLLECTION},${CALIB_COLLECTIONS}"
  dataQuery: "${SELECTION_PTC}"
