description: obs_subaru/HSC bias calibration construction
instrument: lsst.obs.subaru.HyperSuprimeCam
imports:
  - location: resource://lsst.cp.pipe/resources/pipelines/_ingredients/cpBias.yaml
tasks:
  cpBiasCombine:
    class: lsst.cp.pipe.CalibCombineTask
    config:
    - checkNoData: False
      censorMaskPlanes: False
